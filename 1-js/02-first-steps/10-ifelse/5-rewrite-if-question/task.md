Nilai penting: 5

---

# Tulis ulang 'if' menggunakan '?'

Tulis ulang `if` menggunakan operator kondisional `'?'`:

```js
let result;

if (a + b < 4) {
  result = 'Below';
} else {
  result = 'Over';
}
```
